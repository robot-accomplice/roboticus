// Fragment for roboticus-site `changelog-updates.ts` (v1.0.8 entry)
// Merge these strings into the existing 1.0.8 geek/layman bullets if the site
// entry is already drafted; otherwise append as new fields per site convention.

export const reflectWorkingMemoryGeek =
  "Agent reflect/continuation: TOTOF and continuation briefs are trailing system overlays on full session history (no synthetic scaffold that dropped prior turns). CONTINUE_EXECUTION is line-anchored and scrubbed from user-visible assistant persistence.";

export const reflectWorkingMemoryLayman =
  "The agent keeps the full conversation in view when it reflects after tool use or continues a task, so it is less likely to “forget” what you already said. Internal continue markers no longer show up in the chat you see.";
