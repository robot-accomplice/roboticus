# v1.0.6 Release Notes

**Date**: 2026-04-16
**Tag**: `v1.0.6`
**Branch**: `release/v1.0.6`
**Diff stats**: regenerated at PR-finalization time via `scripts/release-notes-stats.sh`; see the Provenance section at the bottom of this file for the live command and the snapshot as of the last regeneration.
**Test packages**: 33 green, 0 failures
**MCP release-blocker checklist**: 5 items PASS, 1 item PASS with explicit fixture-level confidence caveat (item 3 — blessed SSE target validated end-to-end against in-tree httptest fixture; cross-vendor SSE not exercised this release). See "MCP Release-Blocker Checklist" section below for the full status matrix.

---

## Summary

Two distinct streams converged into v1.0.6.

**Memory architecture closure.** Milestones 3 (semantic FTS+vector read path) and 8 (relational distillation from enriched episodes) reached acceptance. The agentic memory roadmap is now materially complete except for the operator-driven LIKE retirement gate (M3.3 telemetry surface shipped; deletion deferred until production traces confirm the safety net is dormant).

**Operational hardening.** A v1.0.5 incident (`sudo roboticus daemon stop` failed with opaque launchctl errors, then chowned the user's data dir to root, then a fresh restart hit a broken SQLite migration, then revealed an ambient-rogue-DB bug, then surfaced a Playwright MCP `initialize failed: EOF` with no diagnostic context) cascaded across six unrelated subsystems. v1.0.6 fixes every one of them and lands the diagnostic surfaces (system-warnings dashboard banner, MCP child-stderr capture, pipeline stage liveness watchdog) that turn future incidents from cascades into single-line operator-actionable signals.

Guiding principle for the operational stream: **silent failures aren't really failures from the system's perspective — operators are the only ones who can act, and they need loud signal to act on.**

---

## Phase 1 — Memory Architecture (M3, M8 closure)

| Item | Milestone | Description |
|------|-----------|-------------|
| FTS trigger completeness | M3.1 | Migration 048 closes three real correctness gaps: missing INSERT trigger on `semantic_memory`, missing UPDATE triggers on `procedural_memory` and `relationship_memory`. Idempotent backfill handles rows ingested before the triggers existed. R-AGENT-136 / R-AGENT-137. |
| HybridSearch-first retrieval | M3.2 | Semantic / procedural / relationship / workflow tiers all use HybridSearch (FTS5 BM25 + vector cosine) as the primary read path. LIKE remains as a documented safety net. Per-tier `retrieval.path.<tier>` trace annotation enables M3.3's empirical retirement decision. R-AGENT-138 through R-AGENT-144. |
| Relational distillation | M8 | `EpisodeSummary.Relations` carries canonical (subject, relation, object) triples extracted from assistant answer + evidence. Repeated triples across multiple high-quality episodes are upserted into `knowledge_facts` via the canonical write gate. Anecdote-hijacking guard: minimum 3 episodes, low-quality / failed episodes ignored entirely. R-AGENT-145 through R-AGENT-152. |
| Telemetry-backed LIKE retirement | M3.3 | `AggregateRetrievalPaths` + `RetrievalPathDistribution` give operators an empirical dormancy gate. `IsDormant` requires both fallback share ≤ 1% AND sample size ≥ 200 observations — barely-queried tiers can't be retired on weak evidence. The deletion itself is correctly deferred to operator action. R-AGENT-153 through R-AGENT-157. |

## Phase 2 — Operational Hardening

### DB layer

| Item | Description |
|------|-------------|
| Migration 042 SQLite-compliant | The original `ALTER TABLE ... ADD COLUMN updated_at TEXT NOT NULL DEFAULT (datetime('now'))` violated SQLite's "no non-constant default on ADD COLUMN" rule and failed every fresh install with `Cannot add a column with non-constant default (1)`. Rewritten using the canonical SQLite pattern (ADD nullable + UPDATE backfill); existing installs at schema_version ≥ 42 are skipped by the runner. |
| File permissions to 0o600 | `db.Open()` now chmods the database file plus WAL sidecars (`-wal`, `-shm`) to 0o600 after schema init. Pre-v1.0.6 files inherited the process umask (0o644 on macOS — world-readable). Best-effort: chmod failures (e.g., file owned by another user from a prior sudo invocation) log a warning but don't block boot. R-DBPERMS-1..4. |

### Daemon control

| Item | Description |
|------|-------------|
| PID-file based stop | `roboticus daemon stop` reads `~/.roboticus/roboticus.pid` (or `cfg.Daemon.PIDFile` if set), sends SIGTERM, waits up to 30s for graceful shutdown, escalates to SIGKILL if needed. No sudo. No DB open. Idempotent — stop on a not-running daemon returns nil. Recovers automatically from `kill -9` zombie state. |
| Minimal `NewServiceOnly` constructor | Install / Uninstall / Control / Status no longer call `daemon.New()` (which ran the entire 12-step boot sequence just to construct a service handle). Eliminates the chown-side-effect chain that turned a single sudo invocation into a multi-day recovery story. |
| macOS uses `launchctl bootout` | Bypasses kardianos/service's legacy `launchctl unload` path (which returns "Input/output error" for stale services). Iterates system/ → gui/<uid>/ domains so installed-as-system and installed-per-user paths both resolve. |
| R-DAEMON-PID-1..3, R-DAEMON-STOP-1..4, R-DAEMON-LAUNCHCTL-1, R-DAEMON-NOBOOT-1 | Lock the contract end-to-end. |

### Operator visibility

| Item | Description |
|------|-------------|
| System-warnings collector | Process-singleton at `core.AddSystemWarning` / `SystemWarningsSnapshot`. Dedupes on (Code, Detail) so warnings recorded by multiple subsystems collapse to one operator-visible entry. Concurrent-safe via internal mutex; snapshots are independent copies (safe for HTTP polling). R-WARNINGS-1..6. |
| Boot-time warning prompt | New `bootSystemWarningsBanner` step in the 12-step boot output. Renders any collected warnings just before "Ready" so foreground operators see them immediately. |
| Dashboard banner endpoint | `GET /api/admin/system-warnings` returns `{warnings: [], count: 0}` (never null) for the dashboard's TypeScript types to consume. R-WARNINGS-7..8. |
| Triggers | `WarningCodeConfigDefaultsUsed` (severity HIGH) when `initConfig` falls through to defaults — surfaces the silent-rogue-DB failure mode that produced the v1.0.5 audit findings. `WarningCodeDatabaseCreatedAtPath` (severity HIGH) when `db.Open` lazily materializes a new file at an unexpected path. R-WARNINGS-9..10. |

### MCP

| Item | Description |
|------|-------------|
| Stdio diagnostic surface | `NewStdioTransport` now captures child stderr into a bounded ring-style buffer (most recent 8KB). A second goroutine reaps the child via `cmd.Wait()` so exit code is observable. `StdioTransport.ChildDiagnostic()` composes both into a human-readable summary that `ConnectStdio`'s failure paths fold into the returned error. Pre-v1.0.6 a child crash produced only `mcp: initialize failed: EOF`; post-v1.0.6 the same failure surfaces npm's actual stderr (e.g., `404 Not Found`, `Cannot find module`). R-MCP-DIAG-1..3. |
| Per-call timeout behavior | `Connection` now owns a long-lived receive loop plus per-request pending-call channels. A timed-out `tools/call` fails that call locally, removes it from the pending map, and drops any late response when it eventually arrives. Only real transport failure now poisons the connection. This closes the operator-hostile behavior where one context timeout silently killed the entire MCP session. R-TOOLS-06. |
| Pipeline stage liveness watchdog | `Pipeline.Run` spawns a goroutine that polls `TraceRecorder.CurrentSpan` every 10s. Stages running longer than 20s produce a `WARN pipeline stage running longer than expected` log every probe interval thereafter. Operators can identify stuck stages from live daemon output instead of needing `kill -QUIT` and a goroutine dump. R-PIPE-WATCHDOG-1..4. |
| Agent system-prompt directives | Two new directives close the v1.0.5 `filesystem_count_only` behavioral regression: (a) ATTEMPT then report — agent must call the tool and let the policy engine return the actual decision rather than reasoning preemptively about constraints; (b) honor explicit output-format requests verbatim ("only the number" → deliver that shape, no preface). R-AGENT-PROMPT-1..2. |
| Channel metadata normalization | Adapters now preserve baseline normalized metadata for shared downstream filters instead of forcing inference from partial state. Telegram emits `is_group`, `chat_type`, and `sender_username`; Signal emits `is_group`; Discord emits `is_group` plus `guild_id`; WhatsApp emits `is_group`, `sender_phone`, and `message_type`; Matrix preserves explicit `room_id` and `sender_mxid`. This keeps conversation filters and channel-aware policy on adapter-owned facts rather than heuristics, while still leaving Matrix DM-vs-room classification explicitly open. R-CH-05. |

### Upgrade path

| Item | Description |
|------|-------------|
| Symmetric narration | `applyProvidersUpdate` and `applySkillsUpdate` now print "Downloading X..." before the fetch and a verification line on success — matches the binary update's narration style. R-UPGRADE-1..2. |
| Self-describing checksum errors | Mismatch errors include URL fetched + expected hash + received hash so operators can triage without re-running curl. |
| Config treated as user-owned | By default `roboticus upgrade all` updates only the binary; `providers.toml` and `skills/*.md` are PRESERVED (no fetch, no SHA check, no overwrite — they're user-owned, drift is normal). New `--refresh-config` flag is the documented opt-in escape hatch for users who explicitly want to overwrite local edits with the registry version. Fresh installs (no local file yet) still fetch + verify. R-UPGRADE-3..5. |

---

## MCP Release-Blocker Checklist

5 items pass outright; item 3 passes with an explicit fixture-level confidence caveat (validated end-to-end against an in-tree httptest SSE fixture, not against a cross-vendor production SSE server — see that row for detail and the post-release follow-up). See `docs/testing/mcp-release-blocker-checklist.md` for the full evidence bundle.

| Item | Status | Validated against |
|---|---|---|
| 1. Generic MCP tests pass | ✅ | `go test ./internal/mcp/...` (29 tests, 0 skipped, 0 failures) |
| 2. Blessed stdio target | ✅ | Microsoft `@playwright/mcp` v0.0.70 — initialize OK (server "Playwright" v1.60.0-alpha-1774999321000), 21 tools listed, `browser_close` round-trip succeeded |
| 3. Blessed SSE target | ⚠️ in-tree fixture | `TestSSEReleaseChecklist_FullValidation` — full handshake + round-trip against in-tree httptest fixture. **Confidence-level caveat: SSE transport implementation is validated end to end; cross-vendor SSE compatibility is not claimed for this release.** |
| 4. Startup diagnostics actionable | ✅ | R-MCP-DIAG-1..3 — `EOF` failures now include captured child stderr + exit state |
| 5. Runtime config truthful | ✅ | User's `~/.roboticus/roboticus.toml` Playwright entry corrected from broken `@anthropic/mcp-server-playwright` (404 from npm) to `@playwright/mcp` (the actual published package) |
| 6. Release docs honest about confidence | ✅ | This release notes file explicitly distinguishes implemented-and-validated paths from in-tree-only validation |

---

## Test Coverage

Every regression below corresponds to a row in `docs/regression-test-matrix.md`. The matrix is the contract surface — if it doesn't have a row, the behavior isn't pinned.

| Surface | New regressions | Class |
|---|---|---|
| FTS trigger completeness | R-AGENT-136..137 | L1 |
| HybridSearch-first retrieval | R-AGENT-138..144 | L1 |
| Relational distillation | R-AGENT-145..152 | L1 |
| Telemetry-backed retirement | R-AGENT-153..157 | L1 |
| Upgrade narration + self-describing errors | R-UPGRADE-1..2 | L1 |
| User-owned config preservation | R-UPGRADE-3..5 | L1 |
| DB file permissions | R-DBPERMS-1..4 | L1 |
| Migration 042 idempotency | R-DBMIG-042 (audit) | A |
| Daemon PID file | R-DAEMON-PID-1..3 | L1 |
| Daemon stop semantics | R-DAEMON-STOP-1..4 | L1 |
| launchctl substring matcher | R-DAEMON-LAUNCHCTL-1 | L1 |
| Daemon NewServiceOnly | R-DAEMON-NOBOOT-1 (audit) | A |
| System-warnings collector | R-WARNINGS-1..6 | L1 |
| System-warnings HTTP wire shape | R-WARNINGS-7..8 | L1 |
| System-warnings instrumentation wiring | R-WARNINGS-9..10 (audit) | A |
| MCP stdio diagnostic | R-MCP-DIAG-1..3 | L1 |
| Pipeline stage watchdog | R-PIPE-WATCHDOG-1..3 | L1 |
| Pipeline stage watchdog wiring | R-PIPE-WATCHDOG-4 (audit) | A |
| Agent attempt-then-report directives | R-AGENT-PROMPT-1..2 (audit) | A |
| Soak follow-up fixes (introspection alias, `~` sandboxing, tool-call JSON stripping) | R-TOOLS-INTROSPECT-1, R-SANDBOX-TILDE-1..2, R-LLM-TOOLPARSE-1 | L1 |
| MCP SSE checklist round-trip | R-MCP-CHECKLIST-3 | L1 |
| Prompt-compression comparison soak harness | R-SOAK-04 | L1/L4 |
| Guard retry artifact truth | R-AGENT-158..159 | L1/L2 |
| Early-return contextual guard parity | R-AGENT-160 | L1/L2 |
| Live guard-context population + score precompute | R-AGENT-161..162 | L1/L2 |
| Checkpoint repository lifecycle ownership (save/load/prune + request restore digest) | R-AGENT-163..165 | L1/L2 |
| Reflection fidelity now sourced from turn-owned artifacts (`tool_calls`, `pipeline_traces`) | R-AGENT-166 | L1/L2 |
| Pipeline-owned checkpoint policy now honors operator config (`enabled`, `interval_turns`) instead of ignoring a dead config surface | R-AGENT-167 | L1/L2 |
| Reflection now preserves structured inference metadata (model, ReAct turns, guard outcomes) in stored episode summaries | R-AGENT-168 | L1/L2 |
| Consolidation now consumes recurring `Learnings` from stored `episode_summary` artifacts and promotes them into `semantic_memory` as `episode_learning` | R-AGENT-169 | L1/L2 |
| Reflection now runs after executive-state growth and records the continuity delta (`ExecutiveVerified`, `ExecutiveQuestionsOpened`, `ExecutiveQuestionsResolved`, `ExecutiveAssumptions`) in stored episode summaries | R-AGENT-170 | L1/L2 |
| Executive-state growth only suppresses closure writes on verifier failures that actually undermine evidence trust, avoiding polluted durable state without discarding supported subgoal continuity | R-AGENT-171 | L1/L2 |
| Episode summaries now persist a structured `content_json` payload alongside the compact text summary, preserving turn-state fields for downstream consumers | R-AGENT-172 | L1/L2 |
| Post-turn reflection writes both human-readable `content` and structured `content_json` into `episodic_memory` on the live path | R-AGENT-173 | L1/L2 |
| Consolidation now prefers `episodic_memory.content_json` over reparsing the compact summary string when both are present | R-AGENT-174 | L1/L2 |
| Compatibility callers that only set `MemoryContext` now derive typed `VerificationEvidence` at the session boundary instead of forcing the verifier to parse rendered memory text | R-AGENT-175..177 | L1/L2 |
| Live guard-triggered retry now flows through one detailed helper that owns contextual guard application, fresh-context rebuild, retry prompt injection, and final applied guard-result capture | R-AGENT-178 | L1/L2 |
| Cache-hit responses now run through contextual guards on the live path instead of the weaker text-only cache guard branch | R-AGENT-179 | L1/L2 |
| Guard preset ownership is now centralized: daemon/smoke/parity no longer inject a fixed full chain, and the live pipeline resolves `GuardSetFull` / `GuardSetCached` / `GuardSetStream` / `GuardSetNone` through one authoritative registry-backed selector | R-AGENT-180..181 | L1/L2 |
| Prompt-layer tool discoverability now matches the selected structured tool surface instead of advertising the daemon boot's full registry | R-AGENT-182 | L1/L2 |
| Request construction now drops empty compacted conversational messages instead of relying on `llm.Service` to scrub them later as a defensive fallback | R-AGENT-183 | L1/L2 |
| Stage 8 `authority_resolution` is now explicitly pinned as the live `SecurityClaim` owner with session attachment, trace annotation, and threat-caution downgrade coverage | R-AGENT-184 | L1/L2 |
| Tool sandbox resolution now uses one shared contract (`ResolvePath` / `ValidatePath`) for `~` rejection, workspace anchoring, and explicitly allowed absolute paths | R-AGENT-185 | L1/L2 |
| Sensitive config mutation protection now uses one shared matcher across pre-execution policy and post-inference guards instead of two drifting sensitive-key vocabularies | R-AGENT-186 | L1/L2 |
| `FilesystemDenialGuard` now honors real sandbox-denial evidence from tool results, so it only strips false capability disclaimers instead of overwriting legitimate path-policy outcomes | R-AGENT-187 | L1/L2 |
| `pathProtectionRule` now targets actual protected path/file markers instead of generic content nouns like `secret`, leaving sensitive config-field protection to the shared config-protection matcher | R-AGENT-188 | L1/L2 |
| Workspace-only allowlist checks in `pathProtectionRule` now use boundary-safe subtree matching, so `/data/vault` no longer accidentally admits `/data/vaultBackup` | R-AGENT-189 | L1/L2 |
| Post-inference execution / financial truth guards now honor real policy and sandbox denials instead of flattening every denial-shaped answer into a fake-capability case, denied financial tool results no longer bless fabricated “I transferred…” claims, and the old canned execution-summary rewrite path has been replaced with regeneration | R-AGENT-190 | L1/L2 |
| Telegram / WhatsApp webhook ingress now uses adapter-owned normalization and verification instead of route-local transport parsing, including WhatsApp challenge verification and signature enforcement on the live route path | R-CH-04 | L1/L2 |
| Plugin runtime is now registry-owned across both startup and install flows: the daemon constructs the registry, scans `cfg.Plugins.Dir`, parses real TOML/YAML manifests into tool definitions, initializes plugins, install/enable flows hot-sync plugin tools plus descriptor embeddings into the semantic tool surface without restart, and manifest-backed plugin scripts now share the same core execution contract as skill scripts | R-TOOLS-03 | L1/L2 |
| Manual cron "run now" execution now reuses the durable `CronWorker` lifecycle, so lease acquisition, retry bookkeeping, run-history recording, and next-run updates are no longer bypassed on the live route path | R-SCHED-05 | L1/L2 |
| Cron run recording now writes the authoritative `cron_runs(error_msg, timestamp)` schema directly instead of carrying legacy fallback branching in the worker hot path | R-SCHED-06 | L1/L2 |
| Daemon-owned memory consolidation no longer runs on a bespoke ticker in `daemon_subsystems.go`; it now reuses `HeartbeatDaemon` + `MemoryLoopTask`, and its cadence is owned by heartbeat config/fallback policy instead of a hard-coded 1h loop | R-SCHED-07 | L1/L2 |
| `/api/traces/search` no longer relies on SQL `LIKE` over serialized trace blobs for tool filtering; tool matches now come from exact `tool_calls` rows and guard filtering is evaluated over parsed JSON in process | R-RT-12 | L1/L2 |
| Model exercise/baseline now uses one runtime truth surface: `/api/models/exercise` runs through the same pipeline-owned request path as `roboticus models exercise`, with `NoCache` and `NoEscalate` preventing cache/fallback contamination | R-RT-13 | L1/L2 |
| Streaming `NoEscalate` requests now skip cache replay just like non-streaming `Complete`, so benchmark/raw-capability paths are not contaminated by cached content in one mode but not the other | R-RT-14 | L1/L2 |
| MCP per-call timeout no longer tears down the whole connection; late responses are dropped and follow-on calls continue on the same transport | R-TOOLS-06 | L1/L2 |
| Adapters now emit baseline normalized metadata (`is_group` plus transport-specific IDs where authoritative, and explicit Matrix room/sender identifiers otherwise) so shared filters do not have to guess channel context from missing state | R-CH-05 | L1/L2 |
| Pipeline cache reads now honor `expires_at`, pipeline cache writes stamp explicit TTL metadata, and cache timestamps use one SQLite-friendly format instead of mixed encodings that could leave stale rows effectively timeless | R-PIPE-07 | L1/L2 |

---

## Known Open Items (post-v1.0.6)

These were deliberately deferred to keep the release scoped and grounded:

- **M3.3 LIKE-block deletion**: telemetry surface (`AggregateRetrievalPaths`) is in place; the actual deletion in `retrieval_tiers.go` waits for production traces to demonstrate dormancy per the documented retirement procedure.
- **External behavioral soak rerun pending**: the three pass-5 residuals (`introspection_discovery`, `tilde_distribution`, `filesystem_count_only`) are now addressed in code via an introspection alias, explicit `~` refusal in both tool resolution and policy evaluation, and stripping parsed tool-call JSON from assistant content. Release confidence should be refreshed with another managed clone/fresh soak pass before declaring the behavioral matrix fully closed.
- **Fresh-state soak lane**: the managed `fresh` lane now boots from an isolated generated default config rather than a copied operator config, so clean-state validation is finally meaningful. Long-duration fresh-state soak evidence is still an rc validation artifact rather than something pinned in this notes file.
- **Prompt compression remains suspect until paired-soak evidence exists**: the repo now includes `scripts/run-prompt-compression-soak.py`, which runs the same live scenarios with prompt compression forced `off` and `on` against isolated configs and treats pass→fail drift as a regression. That harness is the closure bar for prompt compression; wiring the feature is not sufficient evidence of quality.
- **Prompt compression is intentionally narrower than the old Rust-era surface**: the current Go implementation compresses only older conversational history and leaves system prompt, memory, ambient system notes, and tool payload messages verbatim. This is deliberate protection for the richer Go memory architecture, not an accidental divergence.
- **Episode summaries now have a dual surface by design**: `episodic_memory.content` remains the compact human-readable summary, while `content_json` preserves the richer structured turn-state used by consolidation. This is intentional long-term architecture, not temporary duplication.
- **Rendered memory text is no longer verifier-owned state**: compatibility callers may still derive typed evidence from `MemoryContext`, but that normalization now lives at the session boundary. Downstream consumers stay on typed artifacts.
- **Guard-triggered retry now has one live owner**: the standard inference path delegates guard retry to `retryWithGuardsDetailed(...)` instead of maintaining a second hand-rolled implementation in `pipeline_stages.go`. The live path still preserves its historical “one extra attempt, then accept the final guarded content” behavior via explicit retry policy.
- **Cache-hit guards now use live session context**: cached responses no longer bypass contextual guards through `ApplyFull(...)`; the cache-hit path now applies `ApplyFullWithContext(...)` with the live `GuardContext` and cached model metadata.
- **Guard preset selection is now real on the live path**: the daemon, smoke test, and parity integration path no longer inject a fixed full chain that silently overrode `GuardSetCached` / `GuardSetStream` / `GuardSetNone`. The pipeline now resolves presets through one centralized `GuardRegistry`, while still allowing explicit custom chains for tests or specialized callers.
- **Prompt tool instructions now match the injected tool surface**: the model's textual tool roster is now rewritten from the same selected per-request tool defs that populate `llm.Request.Tools`, so the prompt no longer advertises full-registry capability while the structured request carries a pruned set.
- **Plugin runtime is no longer split between startup and install**: daemon startup owns registry construction, directory scan, manifest parsing, init, and `AppState.Plugins` wiring, and `/api/plugins/install` now writes a manifest-backed plugin directory and hot-loads it into that same live registry without restart. Startup-loaded and install-loaded plugins now share one runtime-owned surface.
- **Hot plugin tool loading now reaches semantic pruning too**: install/enable flows no longer stop at registry sync. They also refresh tool descriptor embeddings, so newly loaded plugin tools participate in the same ranked/pruned request surface as startup-loaded tools instead of remaining rankless until daemon restart.
- **Plugin scripts no longer run under a weaker contract than skill scripts**: skills and manifest-backed plugin tools now share one core script execution contract for root containment, interpreter allowlists, output truncation, and sandbox env shaping. Plugin-specific env injection remains, but the execution policy no longer drifts across the two runtimes.
- **Manual cron execution is no longer a lifecycle bypass**: `/api/cron/{id}/run` now routes through `CronWorker.RunJobNow(...)` instead of calling `RunPipeline` directly from the route, so lease/run-history/retry ownership is consistent between scheduled and operator-triggered execution.
- **Empty compacted history is now dropped at the right boundary**: `ContextBuilder.BuildRequest` no longer emits blank user/assistant messages when compaction collapses filler to nothing. `llm.Service` still keeps its defensive scrub, but request construction now owns the correctness of the artifact it produces.
- **Sandbox path semantics are now less fragmented**: filesystem tools and generic sandbox validation share the same path-resolution contract, and live sessions now snapshot `AllowedPaths` at creation/load time instead of sharing the pipeline slice header. This materially reduces cross-layer sandbox drift without changing the operator-visible contract.
- **Config protection is now less fragmented too**: the policy engine and `ConfigProtectionGuard` now consume one shared sensitive-config matcher, which closes a real drift seam where the two paths had different protected-key vocabularies.
- **Filesystem-denial truth is less brittle**: `FilesystemDenialGuard` now checks for real sandbox/path-denial evidence in the tool results before rewriting denial language, which keeps the guard aligned with actual policy outcomes instead of treating every filesystem disclaimer as false.
- **Path protection is less noisy**: `pathProtectionRule` no longer misfires on ordinary payload text like `secret`; actual protected file/path markers stay in the path rule, while sensitive config fields are owned by the shared config-protection matcher.
- **Workspace-only allowlists are less error-prone**: policy-layer allowlist checks now use the same exact-match-or-subtree semantics as the runtime path resolver, closing a boundary bug where sibling paths could slip through on prefix matches.
- **Cross-vendor SSE MCP target**: validated only against in-tree fixture this release. If a production third-party SSE MCP server becomes available before v1.0.7, the checklist's item 3 should be re-run with that target named.

---

## Upgrade Notes

- **Existing installs**: the DB file permission tightening (`db.Open` → 0o600) auto-applies on next boot. No operator action required.
- **Existing installs with rooted files**: if `~/.roboticus/state.db` or any file in the data dir is owned by `root` (from a pre-v1.0.6 `sudo roboticus daemon stop`), the chmod will warn-but-not-block. Run `sudo chown -R "$(id -u):$(id -g)" ~/.roboticus` to restore ownership; future invocations will not need sudo.
- **`roboticus upgrade all` behavior change**: providers.toml and skills/*.md are now PRESERVED by default. Pass `--refresh-config` to opt into the previous overwrite behavior. Behavior change is documented in the command's `--help` output.
- **`roboticus daemon stop` no longer needs sudo** when stopping a daemon you started yourself (`roboticus serve` or via launchd in user-mode). System-mode launchd installs still need sudo for the underlying `launchctl bootout`.
- **MCP config**: if your `roboticus.toml` references `@anthropic/mcp-server-playwright`, replace with `@playwright/mcp` — the original package name returns 404 from npm.

---

## Provenance

Branch: `release/v1.0.6`.

Snapshot numbers (commit count + file diff) are drift-prone by
nature — the first round of release notes landed with "49 commits,"
the second with "61 commits," and each subsequent audit-closure batch
invalidated the number again. Rather than tempting a third round of
drift, this block documents the regeneration command so the numbers
can be rebuilt from git at PR-finalization time, right before merge:

```bash
# Run at final PR head:
scripts/release-notes-stats.sh v1.0.5
```

Output is the multi-line block that gets pasted under this heading.
The script is a single grep-and-awk wrapper — 10 lines, no external
deps — so regeneration is friction-free.

**Most recent regeneration** (2026-04-16 self-audit closure, AFTER
the 12 post-first-review findings landed):
- Commits since v1.0.5: 63
- Files changed: 172
- Insertions / deletions: +23,267 / −805
- Regressions added between initial release branch cut and audit
  closure: R-WIN-UPDATE-1, R-SVC-INSTALL-1..3, R-FW-MIGRATION-1..3,
  R-INTENTS-CTX-1..4, R-MEM-PIPELINE-OWNED-1..2, R-INSTALL-REPO-PARITY-1,
  R-INIT-WORKSPACE-1..3, R-INSTALL-CHECKSUM-1, R-INSTALL-GH-PARSE-1,
  R-WIN-PATH-1, R-WIN-SIDECAR-RESERVE-1..5, R-INSTALL-PATH-ENV-1..2,
  R-SIDECAR-SWEEP-1..4

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
